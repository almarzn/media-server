{{- if .Values.radarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-server.fullname" . }}-radarr
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "media-server.labels" . | nindent 4 }}
    {{- include "media-server.componentLabels" "radarr" | nindent 4 }}
spec:
  type: NodePort
  ports:
  - port: {{ .Values.radarr.port }}
    targetPort: http
    nodePort: {{ .Values.radarr.nodePort }}
    name: http
  selector:
    {{- include "media-server.selectorLabels" . | nindent 4 }}
    {{- include "media-server.componentLabels" "radarr" | nindent 4 }}
{{- end }}